
1. 对于用pandoc转md生成的html，其宽度最大是固定的，这个看能不能加载的时候只加载body，忽略pandoc自带的那些无趣的信息。
2. 代码字体的颜色，还有代码块的主题色，这个看有没有现成的css方案
3. 图片点击放大看能不能实现以下，现在点击无反应。
4. nginx的配置文件/etc/nginx/conf.d/my-static-site.conf 还有/etc/nginx/nginx.conf（第一行的那个mindul），这些个性化的配置最好写好wiki记录起来
5. 文档载体除了markdown，看能不能用tex、texinfo、typst等等写，写完转成html。反正大家都是信息载体，html是最好的展示方式。然后编译框架看能不能把这几种都加进来
